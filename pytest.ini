[pytest]
testpaths = tests
base_url = https://www.saucedemo.com
markers =
    smoke: fast critical-path checks
    regression: full regression suite
    feature: allure feature label
addopts = -ra --strict-markers
